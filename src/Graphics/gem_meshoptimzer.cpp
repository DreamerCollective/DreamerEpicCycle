

#include "gem_meshoptimzer.h"

void gem_meshoptimzer::Meshoptimzer()
{
//    Mesh mesh;
//    if(mesh.IodCount < COUNTOF(mesh.Lods))
//    {
//        size_t nextIndicesTarget = size_t(double(LodIndices.size()) * 0.75);
//        size_t nextIndicesTarget = Meshopt_simplify(lodIndices.data(), lodIndeices.data(), lodIndices.size(). &vertices[0].vx, vertices.Size(), sizeof(Vertex), nextIndicesTarget, 1e-2f);
//        assert(nextIndices <= lodIndices.size());
//
//        if(nextIndices == IodIndieces.size())
//            break;
//
//        LodIndices.resize(nextIndices)
//        meshopt_optimzeVertexCache(LodIndices.data(), LodIndices.data(), LodIndices.size(), vertex_count)
//    }
}